# April 分支协作规则

## 写入前确认

任何会修改文件或生成产物的动作，执行前必须先向用户列明并等待明确确认：

- 拟修改范围：具体文件、目录或将生成的产物。
- 修改内容：准备改什么、为什么改、预期影响是什么。
- 不修改内容：明确哪些文件、规则、测试、输出或 git 操作不在本次范围内。
- 验收方式：用什么命令、报告或人工检查确认结果。

用户说“保存”“沉淀”“完善”“继续推进”“开始处理”不等于已确认写入；只有用户明确表达“确认修改”“可以改”“开始改”“按这个范围执行”等，才可进行写入。

## Git 操作

`git add`、`git commit`、`git push` 必须单独列清单并等待确认，即使代码修改已经获得确认。

默认目标分支为 `April`。未经用户确认，不切换到其他成员分支，不推送到 `main`。

## 现场赛优先级

比赛时间有限，优先交付能跑通的最小闭环：

1. 明确业务痛点和输入输出。
2. 完成可演示 Demo。
3. 生成清晰结果或报告。
4. 补充 README 和展示话术。
5. 最后再考虑界面美化和扩展能力。

## 技术选择

默认优先使用 Python + Streamlit 完成 POC。若题目明显偏产品系统界面，再考虑 React + Ant Design 或 shadcn/ui。

依赖安装、外部模板引入、第三方代码复制前，必须说明来源、用途、许可证风险和替代方案。
